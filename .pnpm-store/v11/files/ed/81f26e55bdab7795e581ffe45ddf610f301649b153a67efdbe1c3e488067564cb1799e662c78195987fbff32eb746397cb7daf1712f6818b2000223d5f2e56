/**
 * Convert a CSS size value to rem.
 */
export declare function convertToRem(size: string): number;
/**
 * Determine if a package is exempt from externalization.
 */
export declare function isExemptPackage(id: string): boolean;
/**
 * Determine if a value is a CSS-wide keyword.
 */
export declare function isCssWideKeyword(value: string): boolean;
